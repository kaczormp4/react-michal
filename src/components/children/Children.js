import React from 'react'
import Title from './Title';

const Children = ({ data }) => {
    // console.log(props.data);
    return (
        <div>
            Children
            <Title data={data} />
        </div>
    )
}
export default Children